import { StyleSheet } from 'react-native';


const style = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    width: '100%',
    overflow: 'hidden'
  },
  container: {
    position: 'relative'
  }
});

export default style;

